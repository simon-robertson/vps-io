import React from "react"

import createComponent from "core/factories/createComponent"

const PageContent = createComponent("memo:page-content", () => {
    return (
        <div className="page-content"></div>
    )
})

export default PageContent
